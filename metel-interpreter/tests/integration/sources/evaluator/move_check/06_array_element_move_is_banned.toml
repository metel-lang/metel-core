[options]
arch_verifies = ["arch.move-check.requirement-3"]
error = ["T0019"]
move_check = true
spec = ["spec.ownership.partial-moves.which-constructs-support-partial-moves.legality-1"]

[expect]
status = "typecheck_error"
code = "T0019"
contains = "array element moves are not allowed"
